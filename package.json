{
  "name": "carpinteria",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm run check:optimizer && npm --prefix backend run build && npm --prefix frontend run build",
    "sync:optimizer": "node scripts/sync-optimizer.mjs",
    "check:optimizer": "node scripts/sync-optimizer.mjs --check"
  }
}
